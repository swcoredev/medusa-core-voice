## 🧭 Миссия проекта (Обязательно читать перед любым заданием)

### Название проекта:
**`medusa-core-voice`** — кастомный маркетплейс голосовых ассистентов на базе Medusa + PostgreSQL + модульная архитектура.

---

### 🎯 Цель:
Построить **собственный голосовой маркетплейс** ассистентов, который:
- не использует `medusa-starter-default`;
- построен **с нуля** на `medusa-core`;
- работает на PostgreSQL (порт `5433`, контейнерный);
- разделён на **модули и рабочие пространства** (pnpm workspaces);
- масштабируется под подключение ассистентов (стоматолог, отель и т.д.).

---

### 🔧 Архитектура:
- `packages/backend` — ядро Medusa + настройки
- `PostgreSQL` — используется через Docker (порт `5433`)
- Ветки:
  - `main` — продакшен
  - `staging` — рабочая
  - `developer-cursor` — ветка для Cursor

---

### 🧠 Правила для Cursor:
1. **Никогда не отклоняйся от архитектуры.**
2. Всегда проверяй, где ты работаешь — `developer-cursor`.
3. Перед каждой задачей заглядывай в эту миссию.
4. Не использовать готовые шаблоны (`starter`), всё кастом.
5. Учитывай, что фронтенд будет работать через **Vercel**.
6. Все действия должны быть совместимы с монорепой (`pnpm workspaces`).

---

### 📝 Что ты делаешь сейчас:
Cursor работает над установкой и отладкой backend-части `medusa-core-voice`, подключает PostgreSQL в Docker, устраняет ошибки и готовит проект к созданию кастомных endpoints и модулей.

---

Если ты не понял миссию — **спроси.** Не начинай задание без уверенности в направлении.


🔄 Твоя модель:

Разработчик выкладывает,
Ты продаёшь от своего имени
(он как “невидимый поставщик”, ты — как “бренд”).

⸻

🔹 Как это называется:

Это называется “Managed Marketplace” или “Seller via Admin”
	•	Разработчики (ассистенты) не имеют своего storefront,
	•	Но загружают ассистентов через свой кабинет или по API,
	•	Все ассистенты отображаются в твоём общем магазине (B2C) под твоим брендом,
	•	Ты контролируешь витрину, описание, цены, качество,
	•	Деньги идут на твой аккаунт, потом (если нужно) делишься по договору.

⸻

✅ Преимущества:
	•	Ты сохраняешь контроль над магазином,
	•	Разработчики не мешают дизайну, бренду, UX,
	•	Ты главный владелец клиента (email, подписка, поведение),
	•	Можно легко масштабировать на десятки ассистентов.

⸻

⚙️ Как это делается на Medusa:

Компонент	Что делает
Admin panel	Только ты видишь и управляешь всем
Seller (Developer)	Может прислать ассистента через API или вручную загрузить (через упрощённую форму, которую ты создашь)
Product Owner	Ты, как владелец всех ассистентов
Marketplace Logic	Опционально: в базе можно сохранять кто разработчик, но не показывать это в интерфейсе
Revenue sharing	По желанию — вручную или автоматом считаешь проценты, если договоришься


⸻

📦 Пример сценария:
	1.	Max (разработчик) присылает тебе модель или ты даёшь ему панель загрузки (UI).
	2.	Он загружает ассистента (описание, фичи, демо-сценарии).
	3.	Ты проверяешь, активируешь, публикуешь.
	4.	Клиенты видят ассистента как будто это твой продукт.
	5.	Все заказы идут на твой аккаунт.
	6.	Если надо, ты потом отчисляешь разработчику вручную или через отчёт.

⸻

💥 Вывод:

🔹 Это идеальный старт:
– магазин твой,
– продавцы “за кулисами”,
– полный контроль,
– максимум гибкости.

⸻

Хочешь — я подготовлю для Cursor’а задание на такую структуру:
	•	магазин от одного владельца,
	•	с возможностью добавлять ассистентов от разных разработчиков
(скрыто, но с учётом в базе, чтобы потом подключить аналитику и автоматизацию).

Дать такое задание?

————————————————————————————————

Курсор предложения для развития разработки

 достижения конечной цели - **Managed Marketplace голосовых ассистентов**.

🧠 Не менять структуру, не добавлять storefront. Работать строго как micro-MVP: core + кастомный фронт.

## �� **Логическая цепочка разработки**

### **Этап 1: Расширение Backend API (Сейчас)**
**Цель:** Создать полноценный API для управления голосовыми ассистентами

#### **Что нужно добавить:**
1. **CRUD операции для ассистентов:**
   - `POST /api/voice-assistants` - создание ассистента
   - `PUT /api/voice-assistants/:id` - обновление
   - `DELETE /api/voice-assistants/:id` - удаление
   - `GET /api/voice-assistants/:id` - получение одного

2. **API для разработчиков:**
   - `POST /api/developers/register` - регистрация разработчика
   - `POST /api/developers/assistants` - загрузка ассистента
   - `GET /api/developers/assistants` - список ассистентов разработчика

3. **Управление статусами:**
   - `PUT /api/voice-assistants/:id/approve` - одобрение
   - `PUT /api/voice-assistants/:id/reject` - отклонение
   - `PUT /api/voice-assistants/:id/publish` - публикация

### **Этап 2: Admin Panel (Следующий)**
**Цель:** Панель управления для владельца маркетплейса

#### **Функции:**
- Просмотр всех ассистентов
- Одобрение/отклонение заявок
- Управление ценами и описаниями
- Аналитика и статистика
- Управление разработчиками

### **Этап 3: Developer Portal (Параллельно)**
**Цель:** Интерфейс для разработчиков

#### **Функции:**
- Регистрация разработчика
- Загрузка ассистентов
- Отслеживание статуса
- Статистика продаж

### **Этап 4: Customer Marketplace (Финальный)**
**Цель:** Публичный магазин для клиентов

#### **Функции:**
- Каталог ассистентов
- Поиск и фильтрация
- Демо и тестирование
- Покупка и интеграция

## 🚀 **Предложение: Начать с Этапа 1**

### **Почему именно сейчас:**
1. ✅ **Backend уже работает** - есть основа
2. ✅ **База данных готова** - таблицы созданы
3. ✅ **API endpoints есть** - можно расширять
4. ✅ **Архитектура понятна** - следуем Request.md

### **Конкретный план действий:**

#### **1. Расширить API для ассистентов:**
```javascript
// Добавить в simple-server.js:
- POST /api/voice-assistants (создание)
- PUT /api/voice-assistants/:id (обновление)
- DELETE /api/voice-assistants/:id (удаление)
- GET /api/voice-assistants/:id (получение одного)
```

#### **2. Добавить API для разработчиков:**
```javascript
// Новые endpoints:
- POST /api/developers/register
- POST /api/developers/assistants
- GET /api/developers/assistants
```

#### **3. Управление статусами:**
```javascript
// Статусы ассистентов:
- pending (ожидает одобрения)
- approved (одобрен)
- rejected (отклонен)
- published (опубликован)
```

## 💡 **Мое предложение:**

**Начать с расширения API для ассистентов** - это логичный следующий шаг, потому что:

1. **Строим на готовом** - используем существующий backend
2. **Следуем архитектуре** - не отклоняемся от Request.md
3. **Готовим основу** - для Admin Panel и Developer Portal
4. **Тестируем логику** - проверяем бизнес-процессы

### **Хотите, чтобы я:**
1. **Расширил API** для управления ассистентами?
2. **Добавил CRUD операции** для голосовых ассистентов?
3. **Создал API для разработчиков**?

—————————————————————————————————

